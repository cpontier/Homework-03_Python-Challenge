import os
import re
import collections

paragraphTxt = os.path.join("raw_data", "paragraph_1.txt")
resultsTxt = os.path.join("output.txt")

#desired output
#Approximate word count
#Approximate sentence count
#Approximate letter count (per word)
#Average sentence length (in words)

#convert txt file into list of individual characters
with open(paragraphTxt) as text:
    datacharacters = list(text.read().split()
    datacharacter
print(datacharacters[1])

